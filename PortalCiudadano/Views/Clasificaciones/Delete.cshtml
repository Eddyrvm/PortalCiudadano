@model PortalCiudadano.Models.LiquidacionPatente.Clasificacion
@{
    ViewBag.Title = "Eliminar Clasificación";
}

<div class="container my-4">
    <div class="card shadow-sm border-danger">
        <div class="card-header bg-danger-subtle d-flex align-items-center gap-2">
            <i class="bi bi-exclamation-triangle-fill text-danger"></i>
            <h5 class="mb-0">Confirmar eliminación</h5>
        </div>

        <div class="card-body">
            <div class="alert alert-warning d-flex align-items-start gap-2" role="alert">
                <i class="bi bi-exclamation-octagon-fill fs-5"></i>
                <div>
                    <strong>¿Estás seguro de eliminar esta clasificación?</strong><br />
                    Esta acción es <u>irreversible</u>.
                </div>
            </div>

            <dl class="row mb-0">
                <dt class="col-sm-4">Nombre de la clasificación</dt>
                <dd class="col-sm-8 fw-semibold">@Model.NombreClasificacion</dd>
            </dl>
        </div>

        <div class="card-footer d-flex justify-content-between">
            <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left-circle me-1"></i> Cancelar
            </a>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.ClasificacionId)

                <button type="submit" class="btn btn-danger">
                    <i class="bi bi-trash me-1"></i> Eliminar definitivamente
                </button>
            }
        </div>
    </div>
</div>